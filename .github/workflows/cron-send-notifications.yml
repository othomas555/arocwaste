- name: Ping cron endpoint
  run: |
    code=$(curl -sS -o /dev/null -w "%{http_code}" \
      "https://www.arocwaste.co.uk/api/cron/send-notifications?secret=${CRON_SECRET}")
    echo "HTTP status: $code"
    test "$code" = "200"
